## Review 1 · 2026-09-23 · codex · plan commit 8c92928
F1 blocking · Probe `3.1` is unanswered: the epic does not define or constrain the shapes, limits, and invalid-input responses for commands, cfg keys, zones, triggers, conditions, actions, or announcement text. — Delegate each input family to a named child with explicit schema/limit/error constraints and a D-item that submits invalid values.
F2 blocking · Probe `3.3` is unanswered: retention and deletion are missing for `LogOutput.log`, `.bak` files, release archives, temporary build/staging files, GitHub releases, and audit records. — State storage owner, retention, deletion mechanism, and single-copy status for every artifact, backed by one command such as `pwsh tools/preflight.ps1 -Check persistence-policy` that fails when the declared artifact inventory is incomplete.
F3 blocking · Probe `4.5` is unanswered: D4, D10, D17, D18, and D21 use “every” without defining complete set derivation, treatment of untracked/new/generated files, or how the checker proves it saw the whole set. — Define each set from an authoritative manifest or repository walk, including untracked/build-created paths, and make `pwsh tools/preflight.ps1 -SelfTest` plant omitted members outside the expected directories.
F4 advisory · Probe `4.3` chooses server-local time but leaves DST gaps, repeated clock times, and manual clock changes unspecified. — State whether a repeated local time fires once or twice and whether skipped times or backward clock changes are replayed.
F5 blocking · Probe `6.1` is unanswered: external-contract sampling across supported record types, quotas, and rate/cost constraints is absent for GitHub, Thunderstore/tcli, VCF, and game/runtime APIs. — Assign each dependency to a named child with supported versions, sampled input/response contracts, quota/cost decisions, and an incompatibility response.
F6 blocking · Probe `6.2` is unanswered: only missing tools, hook renames, and malformed JSON are covered; slow, unavailable, rate-limited, and garbage responses are undecided for GitHub, Thunderstore, tcli, gh, game APIs, and review collaborators. — State fail/timeout/retry/fallback behavior for every dependency and provide one command such as `pwsh tools/preflight.ps1 -SelfTest dependency-failures` that fails when any dependency lacks all four cases.
F7 blocking · Probes `7.1`, `7.2`, and `7.3` are unanswered: there is no policy for partial initialization/loading, concurrent reload/purge/tick/admin actions, retries racing originals, stale edits, interrupted writes, or what a correction invalidates. — Delegate these to `foundation` with constraints defining serialization/locking, idempotency, atomic reload behavior, interruption recovery, and invalidation rules.
F8 advisory · Concrete concurrent scenario: two admins issue `.nyar event reload` and `.nyar purge confirm` while the scheduler starts the same event; neither precedence nor the resulting persisted state is defined (`7.2`). — Specify the synchronization boundary and expected final event, tracker, and `state.json` state.
F9 blocking · Probe `9.3` is unanswered: D11 tests one purge but does not decide duplicate starts, repeated confirmations, double reloads, or a retry racing the original. — Define idempotency keys or duplicate-action semantics for each mutating command and trigger, with a named child required to test them.
F10 blocking · Probe `10.1` is unanswered: D5 checks annotations only and cannot fail when scheduled, trigger, reload, debug, or indirect execution bypasses authorization. — Define authorization for every direct and indirect path and add one command such as `dotnet test --filter AuthorizationEveryPath` that fails when any path executes without its required actor permission.
F11 blocking · Probe `10.3` is unanswered: D9 recognizes a few tracked-file token patterns but does not verify log redaction, environment inheritance, credential rotation, alternate token forms, or generated/untracked artifacts. — State credential storage and rotation procedures plus forbidden log/output locations, and use one command such as `pwsh tools/preflight.ps1 -Check secrets` that scans tracked, generated, staged, and captured-log fixtures and fails on each prohibited exposure.
F12 blocking · Probes `10.2` and `10.4` are unanswered: D15 exercises JSON validity rather than shell/template/path/URL injection, while admin identifiers have no retention, deletion, access, or audit-exposure policy. — Constrain every data-to-sink path and define the personal-data lifecycle, with executable hostile-input tests and a log-retention D-item.
F13 blocking · Probe `11.3` is unanswered: “chat-only” and a 480-byte limit do not decide keyboard, screen-reader, contrast, wrapping, or small-screen behavior. — State the supported accessibility behavior or a tested platform limitation and add a manual D-item covering keyboard-only use, assistive reading, and narrow chat presentation.
F14 blocking · Probes `12.1` and `12.3` are unanswered: failure responses are specified only for rejected events, and production detection relies on an admin noticing log lines rather than an alert or dashboard policy. — Map each failure class to user action and delegate an explicit health/alert mechanism, threshold, owner, and recovery link to `foundation`.
F15 blocking · Probe `12.4` is unanswered: D10 does not enumerate each check’s failing, silent, and empty inputs, does not require fixtures to reproduce real spelling/state, and supplies no single evidence command for each gating control. — Add a gating-control matrix for `2.1`, `3.3`, `4.4`, `6.2`, `10.1`, `10.3`, `14.3`, and `14.4`, naming one independently runnable failing command and exact bad/silent/empty fixture per probe.
F16 blocking · Probe `4.4` states precedence, but no single command fails when that precedence is removed; D11 and D14 test isolated outcomes, not conflicting controls. — Add `dotnet test --filter ControlPrecedence` with simultaneous purge, global-disable, pillar-disable, cap, and definition conflicts and assertions for the stated order.
F17 blocking · Probe `14.3` is unanswered as a control: D11/D12 test cleanup or uninstall, not installation of a previous release, newer-schema behavior, or the claimed tag-to-tag revert range. — Provide one rollback command or scripted test that installs release N, writes data, installs N−1, verifies declared behavior, and fails for an incorrect commit range or incompatible data.
F18 blocking · Probe `14.4` is unanswered as a control: D20 and D22 validate two outputs but cannot fail when the paths inventory omits a shipped, generated, ignored, server-side, or review-created path. — Add one manifest-comparison command that walks every Build-plan step in a fixture, captures all reads/writes/regenerations, and fails on any path absent from `Paths walked`.
F19 blocking · D7 is unverifiable by its stated command because its `fails when` checks only calls outside `Persistence.cs`, not the claim that every resolved path remains beneath the configured data directory. — Add traversal, rooted-path, symlink/reparse-point, and alternate-API fixtures whose command fails if `Persistence` can escape the directory.
F20 blocking · D10 is unverifiable by a stranger because `<n>/<n>` is circular: neither the expected check inventory nor each check’s required bad/good/empty fixture is named. — Commit an authoritative check manifest and make self-test fail when a manifest entry, fixture class, or expected diagnostic is absent.
F21 blocking · Build-plan step 1 cites D3 although it does not build or verify all D3 release surfaces, omits D18 from its `satisfies` list despite promising that check, and step 2 cites D1 without changing repository visibility or default branch. — Correct each step-to-item mapping and add the actual repository/release-surface actions to the steps that claim D1, D3, and D18.
F22 blocking · Probe `15.2` is unanswered: deferred structure damage, integrations, triggers, markers, and UI work point to prose phases rather than a slug or issue. — Give every deferred scope item a backlog slug or issue, or explicitly state that no later item exists and remove the claim that it is deferred.
F23 advisory · Concrete minimal-stretch scenario: installation followed by no command and immediate uninstall may leave seeded `events.json` and cfg data indefinitely, contrary to the intuitive “nothing permanent left behind” outcome (`8.2`). — Clarify that administrator-owned configuration intentionally remains after uninstall, or document an optional data-removal procedure.
F24 advisory · Concrete maximal/abuse scenario: an operator raises caps far beyond 150/20/3, allowing the excluded 300-unit event while D24 measures only `MaxTrackedUnits`; degradation and a hard safety ceiling are not tested (`9.1`, `9.2`, `13.2`). — Define whether caps have immutable maxima and measure the supported raised-cap boundary.
F25 advisory · S-19 is not truly reversible after public tags, GitHub releases, or Thunderstore publication; version renumbering then breaks immutable external references. — Limit the fallback to pre-publication children and define a forward-only correction policy after publication.
F26 advisory · S-20 understates compatibility cost: adding a `[NYAR:*]` mode later may affect parsers that consume the promised stable parse-friendly replies. — Reserve/version the reply grammar now or state that existing reply syntax remains unchanged when the optional mode is introduced.
5/15 layers · 28/49 probes
VERDICT: REVISE
### Dispositions
- F1 · accepted · input families delegated with constraints: foundation (cfg, events.json, command args), event-spawns (SpawnWaves), defended-zones (zones.json); +D29
- F2 · accepted · Design › Data artifact table with owner, retention, deletion and single-copy per artifact incl. log, .bak/.tmp/.corrupt, build outputs, releases, audits
- F3 · accepted · Business rules 5 defines each set's derivation (git ls-files walks, Children parse, git log filter) and self-test plants outside members; D5 D17 D21 D33 reworded
- F4 · accepted · Business rules 6: once per matching local minute via last-fired UTC instant; DST repeat once, skip not replayed; +D28
- F5 · accepted · External dependency table with versions, contracts, quota/cost and incompatibility response; game contracts sampled by spikes/foundation
- F6 · accepted · dependency table states slow/down/garbage behaviour for every dependency incl. disk and Codex; +D25 fault containment
- F7 · accepted · Design › States: main-thread serialisation, partial init, reload snapshot, purge cooldown, .tmp recovery; delegated to foundation with constraints; +D30
- F8 · accepted · purge cooldown suppresses scheduled restarts; main thread orders all actions
- F9 · accepted · Business rules 8 duplicate-action semantics; +D30
- F10 · accepted · indirect paths take no player input (stated in Security); live non-admin check +D26; the suggested dotnet test is not possible for VCF authorization in-process, D5 static + D26 live stand instead
- F11 · accepted · rotation and storage stated; D9 extended to dist/ and build/ zips and more token forms; logs never print env
- F12 · accepted · sinks enumerated (chat, constant file names; no shell/URL/query) with D29 D31; log retention stated (overwritten each boot)
- F13 · accepted · UX states the client owns chat accessibility; colour never carries meaning alone (prose, non-gating)
- F14 · accepted · failure-class table with user action; health line and admin login notice as the alert, delegated to foundation
- F15 · accepted · gating-control matrix with fails-on, silent-on and empty-input per gating probe
- F16 · accepted · +D27 ControlPrecedenceTests via the new Nyarlathotep.Tests project (S-21)
- F17 · accepted · +D32 manual rollback drill from 0.3.0; revert range stated as prev-tag..tag
- F18 · accepted · +D33 paths manifest check; Paths walked expanded
- F19 · accepted · D7 reworded (constant names, no path parameter); +D31 PersistencePathTests for traversal/rooted/reparse
- F20 · accepted · D10 now names tools/preflight-checks.json and the three fixture classes; n equals the manifest count
- F21 · accepted · step mappings corrected: D3 moved to step 11, D18 added to step 1, step 2 states the repo check
- F22 · accepted · deferred items given future plan slugs in Out of scope
- F23 · accepted · config intentionally survives uninstall; README Uninstall section named
- F24 · accepted · hard ceilings in code (Business rules 9); raised-cap run recorded in foundation test results
- F25 · accepted · S-19 fallback limited to pre-publication; forward-only after
- F26 · accepted · S-20: machine-readable mode opt-in per call, existing replies unchanged

## Review 2 · 2026-09-23 · codex · plan commit 8c92928
F1 advisory · Blind rescore: Considered—1 `Purpose & typical use`; 2 `Design › Permissions`; 5 `Interfaces › Internal`; 7 `Design › States`; 8 `Use cases › Minimal stretch`; 9 `Use cases › Maximal stretch`; 11 `Design › UX`; 13 `Performance`; 15 `Out of scope`. Gap—3, 4, 6, 10, 12, 14. No layer is N/A. — Update the coverage table to reflect these statuses and the unresolved probes below.
F2 blocking · Probe `3.3` remains unanswered as a control: D7 detects fenced write APIs but cannot fail when the artifact inventory omits logs, archives, audit records, generated files, or their retention/deletion policy. — Add an authoritative persistence inventory and one command that fails when any produced artifact lacks location, owner, retention, deletion mechanism, and single-copy status.
F3 blocking · Probe `4.5` remains unanswered for manifest-defined sets: a new preflight check omitted from `preflight-checks.json` makes D10 pass, while untracked commands and generated paths remain invisible to `git ls-files`; a stranger cannot verify the claimed completeness. — Derive expected checks and paths independently from repository/build walks, include untracked and generated members, and make self-test plant an omitted member for every derivation method.
F4 blocking · Probe `6.1` remains unanswered: GitHub, Thunderstore/tcli, VCF, and release tooling have versions and broad outcomes but no named child constrained to sample every supported request/response or record type and decide incompatibility handling. — Assign each external contract to a named child with supported versions, sampled record variants, quota/cost limits, and the response to an incompatible variant.
F5 blocking · Probe `6.2` remains unanswered as a gating control: D8 tests only Harmony guard syntax and cannot fail when GitHub, tcli, VCF, disk, game APIs, or review tooling mishandle slow, unavailable, rate-limited, or garbage responses. — Add one dependency-failure command whose manifest covers all four failure modes for every dependency and internal collaborator, with explicit timeout, retry, fallback, and terminal behavior.
F6 blocking · Probe `10.1` remains unanswered as a control: D5 and D26 cover command annotations and live direct commands, but no single test fails if a scheduled, triggered, reload, debug, or other indirect execution path bypasses its required authorization boundary. — Add one authorization-path test enumerating every direct and indirect entry point and failing when any path executes without its declared actor authority.
F7 blocking · Probe `10.3` remains unanswered as a control: D9 scans limited token spellings in tracked/build files but cannot detect credential inheritance, alternate forms, captured logs, staged/untracked artifacts, or violations of the stated rotation and logging policy. — Add one secrets command scanning tracked, staged, untracked/generated outputs, archives, environment fixtures, and captured logs, with fixtures for every prohibited location and credential form.
F8 blocking · Probe `12.4` remains unanswered: the gating matrix substitutes unrelated checks for `3.3` and `6.2`, splits `10.1` across two evidence types, and gives no independently runnable failing command for rollback; D10’s self-declared manifest cannot reveal an omitted check. — Commit an independently derived gating-check inventory and give each gating probe one runnable command with exact bad, silent, and empty fixtures reproducing real input spelling and state.
F9 blocking · Probe `14.3` remains unanswered as a gating control: D32 is a manual observation without an executable command that validates release N→N−1 data behavior or the asserted tag-to-tag commit range. — Add a scripted rollback drill that installs named adjacent releases, writes data with N, installs N−1, verifies declared schema behavior and cleanup, and fails for an incorrect commit range.
F10 blocking · Probe `14.4` remains unanswered: D33 checks only tracked paths, so an omitted ignored build artifact, server-side file, release asset, generated review page, or build-created path passes despite the broader `Paths walked` claim. — Add one manifest-comparison command that executes or traces every Build-plan step in a fixture and fails on any read, write, regeneration, publication, server-side, or review-created path absent from the manifest.
F11 advisory · Concrete maximal-stretch scenario (`13.1`, `13.2`): the supported 500-unit hard ceiling is materially above D24’s measured 150-unit default, while the foundation child merely “records” a raised-cap run without a required bound or pass criterion. — Give the raised-cap test an explicit latency budget and make its result a child DoD item.
F12 advisory · Concrete unauthorized scenario (`2.1`, `10.1`): a malicious server operator can author a definition whose game trigger causes privileged effects, yet the plan labels operators trusted without stating whether definition capabilities are deliberately unrestricted. — State explicitly that filesystem operators are fully trusted to authorize every definition capability, or restrict and test trigger/action capabilities separately.
F13 advisory · Concrete concurrent scenario (`7.2`): main-thread serialization prevents data races, but two admins issuing reload and purge around a scheduler tick can receive order-dependent replies, and the exact observable command ordering is not specified. — Document queue-order semantics and the expected replies, active-event set, tracker contents, and persisted state for the reload–purge–tick permutation.
EARLIER: unresolved F2, F3, F5, F6, F10, F11, F15, F17, F18, F20
9/15 layers · 40/49 probes
VERDICT: REVISE
### Dispositions
- F1 · accepted · rescore noted; the gaps it lists are the blocking findings below, each addressed
- F2 · accepted · +D34 tools/data-inventory.json with a completeness check against Persistence constants and manifest globs
- F3 · accepted · D10 derives the check list from Test-Check* functions both ways; D5 and D33 walk untracked, ignored and server paths
- F4 · accepted · runtime contracts delegated to spikes and foundation; release tooling sampled on the first release by D20 D21 D32; no quota or cost
- F5 · accepted · +D35 DependencyFailureTests through IFileStore/IHookRegistry fakes; matrix 6.2 now D35
- F6 · accepted · +D36 ActionGateway actor table tested for every ActionKind x actor; matrix 10.1 now D36
- F7 · accepted · D9 scans staged, untracked, build outputs, zips and captured logs, more token forms, and forbids env reads
- F8 · accepted · matrix now gives one runnable command per gating probe (D5 D34 D27 D35 D36 D9 D10 D32 D33) and fixtures are real-file copies
- F9 · accepted · D32 is now tools/rollback-drill.ps1 (cmd) incl. tag ancestry and range-in-manifest checks
- F10 · accepted · D33 -Paths walks tracked, untracked, ignored and server paths after build and deploy
- F11 · accepted · D24 adds a 500-unit ceiling run under 15 ms
- F12 · accepted · Permissions: operators fully trusted to author any definition capability
- F13 · accepted · Design › States gives the reload/purge/tick permutations with final state and replies; tested by D30

## Review 3 · 2026-09-23 · codex · plan commit 8c92928
F1 advisory · Blind rescore: Considered—1 `Purpose & typical use`; 2 `Design › Permissions`; 5 `Interfaces › Internal`; 7 `Design › States`; 8 `Use cases › Minimal stretch`; 9 `Use cases › Maximal stretch`; 11 `Design › UX`; 13 `Performance`; 15 `Out of scope`. Gap—3, 4, 6, 10, 12, 14. No layer is N/A. — Update the coverage table to these statuses and the unresolved probes below.
F2 blocking · Probe `3.3` remains unanswered as a control: D34 compares the inventory only with persistence constants and self-declared path globs, so omitting LogOutput.log, GitHub assets, audit outputs, or another producer from both sources passes. — Independently discover every producer and compare its artifacts with an authoritative inventory containing location, owner, retention, deletion mechanism, and single-copy status.
F3 blocking · Probe `4.5` remains unanswered: D10 defines “every preflight check” from `Test-Check*` functions, D18 relies on a self-declared child-to-document mapping, and D33 relies on manifest globs, so an omitted function, mapping, or path is invisible. — Define independent expected-set derivations from DoD/build-plan requirements and the Children list, then self-test each derivation by planting an omitted tracked, untracked, generated, and out-of-directory member.
F4 blocking · Probe `6.1` remains unanswered: the spikes and foundation children sample runtime contracts, but no named child is constrained to sample every supported VCF, GitHub/gh, tcli/Thunderstore, or review-tool request, response, and record variant. — Assign every external contract to a named child with supported versions, sampled variants, quota/cost limits, and the required handling of incompatible variants.
F5 blocking · Probe `6.2` remains unanswered as a control: D35 covers disk, JSON, and hook faults, but not all four slow, unavailable, rate-limited, and garbage modes for the game, VCF, GitHub, tcli, Codex, or other internal collaborators. — Add one dependency-failure command driven by an independently checked dependency manifest that specifies timeout, retry, fallback, and terminal behavior for every dependency and all four failure modes.
F6 blocking · Probe `10.1` remains unanswered as a control: D36 exhausts `ActionKind × actor` table entries but cannot fail when a command, schedule, trigger, reload, debug hook, or Harmony path mutates state without calling ActionGateway. — Add one authorization-path command that independently enumerates every direct and indirect entry point and fails unless each reaches ActionGateway with its declared actor authority.
F7 blocking · Probe `10.3` remains unanswered as a control: D9 scans named token spellings and selected files but does not cover credential inheritance or all actual captured process/server logs, so credentials can violate the declared storage and no-logging policy without failure. — Define permitted credential sources and subprocess inheritance, enumerate every log/archive/output sink, and make one secrets command test prohibited credential forms in each source and sink.
F8 blocking · Probe `12.4` remains unanswered: D10 supplies bad, silent, and empty fixtures only for preflight functions, while D27, D32, D35, D36 and other introduced commands have no uniformly discoverable real-spelling fixtures; the gating matrix is prose, not executable evidence. — Create an independently derived inventory of every introduced check and require each gating command to run exact bad, silent, and empty fixtures that reproduce real input spelling and state.
F9 blocking · Probe `14.4` remains unanswered as a control: D33 snapshots repository and selected server paths after build/deploy, but does not trace reads, review-generated files, plan-store writes, publication, or remote GitHub release assets while every Build-plan step executes. — Add one manifest-comparison command that traces every Build-plan step in a fixture and fails on any read, write, regeneration, publication, server-side, review-created, or plan-store path absent from the manifest.
EARLIER: unresolved F2, F3, F4, F5, F6, F7, F8, F10
9/15 layers · 41/49 probes
VERDICT: REVISE
### Dispositions
- F1 · accepted · rescore recorded; author 15/15 · 49/49 vs reviewer 9/15 · 41/49, shown to the user side by side (layers 3, 4, 6, 10, 12, 14 differ)
- F2 · rejected · advisory by rule — 3.3 is answered at Design › Data (every artifact incl. LogOutput.log, GitHub releases and audits, with location, owner, retention, deletion, single copy) and D34 enforces it for code- and manifest-produced artifacts; independent producer discovery is build work, not a decision
- F3 · rejected · advisory by rule — 4.5 is answered at Business rules 5 (each set's derivation and what it misses), with D10 and D33 walking functions and paths both ways; a further independent derivation is build work
- F4 · rejected · advisory by rule — 6.1 is answered at Interfaces › External (versions, contracts, quota/cost, incompatibility response per dependency, delegated to spikes/foundation/first release); release tooling is process, not a product contract
- F5 · rejected · advisory by rule — 6.2 is answered by the dependency table (slow/down/garbage per dependency) with runtime faults under D35 and D25; rate-limit drills for gh/tcli/Codex are process tooling
- F6 · accepted · D36 extended with Test-CheckGatewayOnly: a direct mutating call outside ActionGateway fails the item
- F7 · rejected · advisory by rule — 10.3 is answered at Security (credential sources: gh's store and TCLI_AUTH_TOKEN in the publishing shell only; the mod holds none and reads no environment) and enforced by D9
- F8 · rejected · advisory by rule — 12.4 is answered by the gating-control matrix (one command per gating probe, bad/silent/empty) and every test/cmd item's fails-when clause
- F9 · rejected · advisory by rule — 14.4 is answered at Rollout › Paths walked and enforced by D33 after build and deploy; tracing every read is out of proportion to a mod repo

