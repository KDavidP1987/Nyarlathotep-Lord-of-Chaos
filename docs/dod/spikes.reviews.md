# Reviews — spikes

## Review 1 · 2026-09-23 · codex · plan commit 4761cec
F1 blocking · Probe `3.1` is unanswered: numeric inputs lack limits and invalid-input behavior for distance, lifetime, seconds, radius, and prefab GUID; D6 samples only five cases and cannot verify “each spike command validates its arguments.” — Specify accepted ranges and responses for every argument, then add an evidence command or exhaustive manual matrix covering every command and boundary.
F2 blocking · Probe `2.1` is not fully controlled: D2 scans only `Commands/SpikeCommands.cs`, so a `[Command]` moved to another file bypasses the admin-only gate, and “public count unchanged from before” has no recorded baseline. — Make `pwsh tools/preflight.ps1` discover every `[Command]` under the source tree, fail any non-allow-listed public command, and record the allow-list/baseline in versioned input.
F3 blocking · Probe `3.3` has no single evidence command that fails when persistence isolation or deletion is absent; D4 and D18 are manual observations, and timestamps cannot establish which files were written. — Add one command that snapshots/hash-compares all in-scope save roots, verifies only the declared spike path changed, and fails after cleanup if that path or another produced artifact remains.
F4 blocking · Probe `4.2` is not verifiable by D5: its chat replies and counts do not prove every spawned unit has the marker, finite `LifeTime ≤ 600`, and `DestroyWhenDisabled`; immediate second-clear behavior also conflicts with a five-per-frame asynchronous queue. — Add an inspect/self-test command that enumerates every spawned entity and fails on any missing or invalid component, and define whether `clear` drains, waits, or reports an active queue.
F5 blocking · Probe `4.4` has no evidence command that fails when precedence is implemented incorrectly; D5/D6 exercise only fragments of `IsReady > Enabled > limits > arguments`. — Add one automated precedence test covering each conflicting pair and exception path, with failure when a lower-precedence rule wins.
F6 blocking · Probe `4.5` is unanswered for “every spike file”: D13’s path/name/namespace set misses spike code placed in another command or helper file, so its command can pass while throwaway code remains. — Define a mechanically complete spike-code set, such as a required marker on every temporary declaration plus repository-wide scanning, and self-test a renamed/out-of-directory planted file.
F7 blocking · Probe `6.2` lacks decisions and a failing command for unavailable or malformed internal collaborators, including preflight, path discovery, Codex review, and audit tooling; S-7’s temporary greps do not cover D13–D15 or establish equivalent failure behavior. — State fail-closed behavior for each collaborator and add one command that simulates each unavailable/garbage result and fails unless the build or close is blocked.
F8 blocking · Probe `7.3` does not decide what a corrected S1–S3 observation invalidates after its verdict has propagated to feature status, research contracts, Epic amendments, or later child planning. — Define the correction/invalidation procedure, including which derived records reopen and which verdict is authoritative.
F9 blocking · Probe `9.2` leaves actionable abuse policy undecided: an admin can automate calls or supply negative, extreme, `NaN`/infinite radius, duration, distance, and arbitrary carrier GUID values. — Define finite ranges, reject non-finite values, bound query work and repeated calls, and name the response for unsafe carrier prefabs.
F10 blocking · Probe `11.3` is unanswered: “chat-only” and “owned by the game client” do not decide keyboard access, screen-reader exposure, contrast, or small-screen truncation for this human-facing command surface. — State the supported accessibility/client behavior and either impose a reply format that works within it or record a tested platform limitation.
F11 blocking · Probe `12.4` covers only three checks although D1–D18 introduce many checks; failing, silent, and empty cases are absent for command authorization, structural fencing, save isolation, unit bounds, input validation, verdict completeness, contracts, paths, secrets, and audit completeness. — Enumerate every introduced check with fail/silent/empty fixtures and provide one evidence command per gating probe whose fixture uses the real spelling and state.
F12 blocking · Probes `3.2` and `5.3` are not verifiable by D7–D10: merely finding `S1 verdict:`, `S2 verdict:`, `S3 verdict:`, or a section heading does not establish that required observations, fields, GUIDs, sources, and contract comparisons exist. — Replace the substring evidence with a schema-aware documentation check or explicit manual checklist that fails for every omitted required field.
F13 blocking · Probe `14.3` is unanswered: the stated range `git revert <step-2 commit>..<step-8 commit>` excludes the step-2 commit and “brings the harness back,” while reverting only step 2 can leave dependent commits and retained helpers/tooling/docs; no command tests rollback after writes. — Specify exact rollback targets and order for every child-created or retained change, including post-write recovery, and add a disposable-worktree command that performs and verifies the rollback.
F14 blocking · Probe `14.4` is not controlled by D15: `-Paths` checks only paths it already knows were walked, so an undeclared write is invisible rather than failing; one session also cannot establish every conditional path. — Make one command derive writes from build steps plus filesystem/repository before-and-after snapshots and fail on any changed path absent from the manifest, including generated, ignored, external, plan, and review paths.
F15 blocking · Probe `14.4` is also unverifiable through D17: checking only for one `Codex verdict:` substring cannot prove every Build-plan step has both audit entries and a verdict, and step 9 cannot evidence its own completed post-audit before D17 is checked. — Give audit entries structured step identifiers and phases, validate the complete 1–9 matrix with a command, and move final validation after the last post-audit but before closure.
F16 advisory · Probe `7.2` states that main-thread handlers and a 1 Hz coroutine “never interleave,” but yielded coroutines can be separated by later commands; clear-versus-tag and clear-versus-empower races are therefore not demonstrated. — Reword the model as serialized callbacks across frames and test command execution while a clear or march queue is active.
F17 advisory · Probe `12.2`’s D12 search can miss an exception when “Nyarlathotep” is more than five lines away or can misclassify an unrelated nearby exception. — Emit a structured session/correlation marker and classify exceptions by logger/source rather than line proximity.
F18 advisory · The Epic’s spikes-specific constraint is reproduced verbatim and the plan refers to the Epic Child constraints, but inherited Business rules are not explicitly included in that reference. — State that both `Child constraints` and inherited `Business rules` in `docs/dod/nyarlathotep.md` govern this child. 7/15 layers · 40/49 probes VERDICT: REVISE
7/15 layers · 40/49 probes
VERDICT: REVISE
### Dispositions
- F1 · accepted · Design › UX › Argument ranges table (every argument, min-max, default; integers only); D6 now runs one below-min and one above-max value per argument
- F2 · accepted · D2 reworded to the repository-wide command check with the allow-list versioned as $script:PublicCommands in tools/preflight.ps1
- F3 · accepted · D4 is now a cmd (preflight -ServerWrites -Since/-AfterCleanup): every file changed under the server directory and LocalLow must match a server or external glob, no other Saves folder may change, and the save must be gone after cleanup
- F4 · accepted · sweep now audits every listed and marked unit for marker, LifeTime ≤ 600 and DestroyWhenDisabled and reports faults (D5); clear semantics defined: queue, 'clear in progress (<k> left)', then 'nothing to clear'
- F5 · rejected · an automated precedence test would need the Nyarlathotep.Tests project, which the foundation child creates, for a four-rule gate that is deleted at close; instead D6 now runs every adjacent precedence pair in-game and Out of scope names the permanent resolver test (Epic D27)
- F6 · accepted · spike code is defined by naming (namespace Nyarlathotep.Spikes, Spike* identifiers, command group spike) and Test-CheckSpikeCode scans every .cs git sees; the D14 bad fixture plants SpikeHelper in Services/
- F7 · accepted · External table gains rows for preflight, Codex and the dod scripts with fail-closed behaviour (a failing preflight stops the step; Codex falls back to subagent then human; a check problem blocks approve/close); no separate simulation command, since each collaborator's failure already blocks the step it gates
- F8 · accepted · Business rules 6 defines corrections: a superseding dated entry, verdict rewrite, contract row update, corrected/discovered Epic amendment and same-day amendment of any child that relied on it; the latest entry is authoritative (D11)
- F9 · accepted · integers only (VCF), finite ranges per argument, radius ≤ 30 bounds the query, the carrier must be a Buff with LifeTime and not on the do-not-spawn list (D6); repeated calls are bounded by the 30-alive limit
- F10 · accepted · Design › UX › Accessibility records the platform limitation (the chat is the game client's, and there is no screen reader) and the reply format (one line, plain text, no colour)
- F11 · accepted · the failing-case table now lists every check the plan introduces (Test-CheckSpikeCode, Test-CheckServerWrites, Test-CheckAuditSteps, the D12 log check, the sweep audit) with fail, silent and empty cases
- F12 · accepted · D7–D10 are now manual checklists that enumerate every required field, replacing the substring evidence
- F13 · accepted · Rollout › Rollback names the exact ranges (whole child: <step-1>^..<step-9>; code only: step 8, proven by the new D19 diff against the step-2 parent)
- F14 · accepted · D4's before/after listing covers every write under the server directory and LocalLow; D15's walk covers every tracked, untracked and ignored repository path
- F15 · accepted · D17 is now a cmd (preflight -AuditOf spikes) checking the full 1–9 pre/post/Codex matrix; step 9 writes its own post-audit before running D17 and closing
- F16 · accepted · States › Concurrency reworded to serialized callbacks across frames; spawns are refused while a clear drains
- F17 · accepted · every spike line is prefixed [nyar-spike]; caught failures log one line with no stack, so D12 counts '   at Nyarlathotep.' frames, now a cmd
- F18 · accepted · the Planned line states that both the Epic's Child constraints and its inherited Business rules govern this child

## Review 2 · 2026-09-23 · codex · plan commit 4761cec (revision after Review 1)
F1 blocking · Probe `3.3` remains unanswered: D4 uses last-write timestamps, so a file created and deleted during a session, a deletion, or a timestamp-restored write is invisible; a stranger cannot verify that only declared artifacts existed.
F2 blocking · Probe `4.4` remains unanswered: D6 is manual evidence and does not exercise `IsReady=false` or every conflicting-rule and exception path; no single evidence command fails when precedence is implemented incorrectly.
F3 blocking · Probe `4.5` remains unanswered: “every spike file” excludes ignored `.cs` files and relies on builders consistently applying Spike-prefixed names, so a valid helper with a neutral name or ignored path escapes D13.
F4 blocking · Probe `6.2` remains unanswered: collaborator failure policies are stated, but no command simulates missing, throwing, silent, malformed, and falsely successful results for preflight, path discovery, audit validation, DoD scripts, and Codex review.
F5 blocking · Probe `9.2` remains unanswered: the 30-unit cap does not bound scripted `empower` or `inspect` calls, so an admin can repeatedly execute radius queries and buff operations without a rate or work limit.
F6 blocking · Probe `12.4` remains unanswered: the failure table covers only five checks, while D2, D3, D6, D10, D11, D15, D16, D18, and D19 introduce additional checks without real-spelling bad, silent-good, and empty fixtures; D14 itself self-tests only two checks.
F7 blocking · Probe `14.3` remains unverifiable: the inclusive revert range is now specified, but no D-item executes it, verifies every child-created or retained change disappeared, or proves recovery remains possible after server writes; a stranger can only inspect the promise.
F8 blocking · Probe `14.4` remains unanswered: D15 walks paths that exist, D4 detects only timestamp-visible files in selected roots, and D17 checks audit text; no single command derives the complete write set from the build and review workflow or detects transient/deleted and undeclared external writes.
F9 advisory · Probe `7.2` states decisions for yielded-coroutine concurrency, but D5 tests only a second clear; it does not verify tag or march during a drain, clear during an active march, or two admins reaching the 30-unit boundary concurrently.
EARLIER: unresolved F3, F5, F6, F7, F9, F11, F13, F14
9/15 layers · 41/49 probes
VERDICT: REVISE
### Dispositions
- F1 · accepted · (in part) D4 is now a before/after snapshot compare (-Snapshot/-Compare) that sees created, changed and deleted files, with SHA-256 over BepInEx/, logs/, save-data-* and LocalLow so a timestamp-restoring write is still seen, and fails if any other Saves folder exists or changed. A file created and deleted within one session remains unseen; accepted in Business rules 5, because 3.3 is about what data remains
- F2 · rejected · unchanged from Review 1 F5: the four-rule spike gate is deleted at close and D6 exercises every adjacent pair plus the kill path in-game; the permanent resolver and its automated test belong to the foundation (Epic D27). Stated in Out of scope
- F3 · accepted · D13's set is now the project's Compile items (dotnet msbuild -getItem:Compile, which includes ignored files) plus git's .cs files; an ignored Compile item fails D13 whatever its name, and a neutral-named tracked file fails D19; D14 plants both
- F4 · rejected · a fault-injection harness for developer tooling is disproportionate for a throwaway child: each collaborator's failure already blocks the step it gates (Interfaces › External), and every preflight check's empty fixture proves missing input fails (D14). Stated in Out of scope
- F5 · accepted · one 1 s server-wide cooldown on every spike command except clear, reply "spike cooldown (<ms> ms)" (Business rules 1, D6)
- F6 · accepted · the failing-case table now lists every check the plan relies on, including the Epic checks D2, D3, D15 and D16 reuse, the D19 diff and the D20 drill; D14 covers all three new checks and fails if a Test-Check function lacks a manifest entry; D5–D11 are stated to be human checklists without fixtures
- F7 · accepted · new D20: a disposable-worktree rollback drill (revert the recorded range, build, preflight, diff against the pre-child commit), run in step 9 before close
- F8 · rejected · a system-wide write tracer is out of proportion; D4 covers the server directory and LocalLow, D15 every repository path, and Rollout › Paths walked now names the two $env:TEMP paths (the snapshot and the drill worktree). Stated in Out of scope
- F9 · accepted · D5 now also runs a tag during a drain and a clear during an active march; two admins are stated to be equivalent to one admin's sequential commands, because handlers are serialized on the main thread

## Review 3 · 2026-09-23 · codex · plan revision after Review 2 (uncommitted draft on 4761cec)
F1 blocking � Probe `3.3` remains unanswered: D4 is a before/after snapshot and explicitly accepts files created and deleted during a session, so it cannot establish every produced artifact, while timestamp-restored writes outside its hashed subtrees also escape detection. � Require one continuously observed or journal-backed evidence command covering every declared root that fails for any undeclared creation, modification, or deletion, including transient artifacts.
F2 blocking � Probe `4.4` remains unanswered: D6 records a manual sequence, but no single evidence command fails when any precedence pair, `IsReady=false`, kill-path exception, or exception-authority rule is implemented incorrectly. � Add an automated precedence harness command covering every adjacent conflict, the commands exempt from `General.Enabled`, and the rule that exceptions require a plan amendment.
F3 blocking � Probe `4.5` remains unanswered: a neutral-named, untracked-not-ignored `.cs` Compile item passes D13, and if removed before close is invisible to D19; consequently �every spike file� is still derived partly from naming rather than exhaustive membership. � Mark spike membership mechanically or compare the complete captured build-input set with the pre-harness set, covering tracked, untracked, ignored, generated, renamed, neutral-named, and subsequently deleted sources, with fixtures for each route.
F4 blocking � Probe `6.2` remains unanswered: the plan explicitly excludes collaborator fault injection, and no command simulates missing, throwing, silent, malformed, or falsely successful results from preflight, path discovery, audit validation, DoD scripts, and Codex review. � Add one fail-closed collaborator self-test command whose faithful fixtures prove each unavailable or garbage result blocks the affected build, review, approval, or close operation.
F5 blocking � Probe `12.4` remains unanswered: D14 self-tests only manifested `Test-Check*` functions, while D6 precedence, D19 diff, D20 rollback, D12 log inspection, and the sweep audit lack faithful failing, silent-good, and non-passing-empty fixtures; several gating probes also lack a single failing evidence command. � Inventory every introduced executable check and add one self-test command with real-spelling bad, good, and empty fixtures, including one evidence command for each gating probe and its stated `fails when`.
F6 blocking � Probe `14.3` has an unverifiable rollback item: D20 checks repository state only, does not reproduce or verify server/external cleanup, does not separately exercise removal of retained helpers, and refers to a step-9 commit while the drill itself occurs inside step 9, so a stranger cannot identify a necessarily committed immutable range. � Record immutable start/end commits before the drill, then use a disposable environment to revert that inclusive range, regenerate derived files, verify repository and external cleanup after representative writes, and separately test reverting the retained helpers.
F7 blocking � Probe `14.4` remains unanswered: D15, D4, D17, and D20 divide coverage among existing repository paths, selected server roots, audit text, and rollback state; no single command derives and audits the complete workflow write set, including transient paths, LocalLow, temporary snapshots/worktrees, plan/review artifacts, and deleted outputs. � Add one before/after manifest-audit command driven from every Build-plan step that monitors repository, generated, ignored, server, external, temporary, plan, audit, and review paths and fails on any changed or transient path absent from the manifest.
F8 advisory � Probe `7.2` states a serialization policy and D5 now tests most drain interleavings, but it still uses one admin and never demonstrates two admins reaching the shared 30-unit boundary concurrently. � Add a two-admin session or deterministic main-thread harness proving one serialized request succeeds, the other receives the specified limit/cooldown response, and the final entity count remains 30.
EARLIER: unresolved F1, F2, F3, F4, F6, F7, F8, F9
10/15 layers � 43/49 probes
VERDICT: REVISE
### Dispositions
- F1 · rejected · advisory by rule — 3.3 is answered at Business rules 5 and D4: the snapshot compare sees every created, changed and deleted file under the declared roots, hashing where content matters; a file created and deleted within one session leaves no data, which is what 3.3 asks about. A continuous journal (ETW or a file-system watcher) over the server directory is out of proportion for a throwaway child
- F2 · rejected · advisory by rule — 4.4 is answered at Business rules 4 and D6, which runs every adjacent precedence pair and the kill path in-game; the permanent resolver and its automated test are the foundation's (Epic D27), stated in Out of scope. Unchanged since Review 1 F5 and Review 2 F2
- F3 · accepted · D19 now also compares the project's complete Compile-item set with the list captured at the step-2 parent (docs/audits/spikes-compile-base.txt, written in step 2), so a neutral-named source file fails whether tracked, untracked or ignored; a source deleted before close leaves no code behind, which is what 4.5 asks
- F4 · rejected · advisory by rule — 6.2 is answered at Interfaces › External: each collaborator's failure blocks the step it gates, and every preflight check's empty fixture proves missing input fails (D14); a fault-injection harness for developer tooling is in Out of scope. Unchanged since Review 2 F4
- F5 · rejected · advisory by rule — 12.4 is answered at Failure & observability: the table lists every check with its fail, silent and empty cases; the preflight checks carry fixtures (D14), and the D19 diff, the D20 drill and the D12 log check each state their failing input in "fails when:". D6 and the sweep audit are manual checklists, not checks
- F6 · accepted · D20 now reverts <pre-child>..<drill base>, both recorded in docs/audits/spikes.md before the drill (the drill base is HEAD after step 9's pre-audit commit), and also drills removing only the kept helpers (checkout of EntityExtensions.cs from the step-2 parent, then build); the world side is D4 -AfterCleanup
- F7 · rejected · advisory by rule — 14.4 is answered at Rollout › Paths walked, which names every path each step writes, including the two $env:TEMP paths and the review artifacts; D15 walks the repository and plugin paths, D4 the server directory and LocalLow. A system-wide write monitor is in Out of scope. Unchanged since Review 2 F8
- F8 · rejected · advisory — 7.2 is answered at Design › States: handlers are serialized on the main thread, so two admins are one admin's sequence; the owner is the only admin available for the in-game sessions

## Review 4 · 2026-09-23 · human · plan commit ccd028f
F1 advisory · Coverage: the project owner, reading the review page and Codex rounds 1-3, finds every Considered layer answers its probes and every pointer answers its probe; the declined asks (continuous write journal, automated precedence test, tooling fault injection, system-wide write monitor, two-admin session) are accepted as out of proportion for a throwaway child; no unhandled scenario named; every item verifiable by its evidence type — no blocking gap
15/15 layers · 49/49 probes
VERDICT: READY
### Dispositions
- F1 · accepted · no change; anything the spikes reveal is recorded as an amendment during the build
